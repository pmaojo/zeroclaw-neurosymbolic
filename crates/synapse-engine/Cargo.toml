[package]
name = "synapse-engine"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Minimal synapse engine compatibility shim for ZeroClaw memory backend integration."

[lib]
path = "src/lib.rs"
